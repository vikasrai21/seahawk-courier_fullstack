{
  "name": "seahawk-fullstack",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend && cp -r frontend/dist backend/public && npm install --prefix backend && cd backend && npx prisma generate",
    "start": "cd backend && npx prisma migrate deploy && node server.js"
  }
}