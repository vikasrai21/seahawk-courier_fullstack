{
  "name": "seahawk-fullstack",
  "version": "1.0.0",
  "scripts": {
    "build": "npm ci --prefix frontend && npm run build --prefix frontend && cp -r frontend/dist backend/public && npm ci --prefix backend && cd backend && npx prisma generate",
    "start": "cd backend && npx prisma migrate deploy && node server.js"
  }
}