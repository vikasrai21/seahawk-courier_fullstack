[phases.install]
cmds = [
  "npm install",
  "cd ../frontend && npm ci"
]

[phases.build]
cmds = [
  "cd ../frontend && npm run build",
  "npx prisma generate"
]

[start]
cmd = "npx prisma migrate deploy && node server.js"